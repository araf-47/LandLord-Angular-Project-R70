/**
 * Stopgap for "who is logged in" on real-backend owner pages, ahead of real
 * auth (Phase 7 — same hold applies to BariVara). Mirrors current-tenant.ts.
 */
export const CURRENT_OWNER_ID_REAL = 1;
